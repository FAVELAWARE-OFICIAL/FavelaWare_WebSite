# Hooks

Esta pasta contém custom hooks React para lógica reutilizável.

## Exemplos de hooks que podem ser criados:
- `useScrollPosition.js` - Hook para detectar posição do scroll
- `useWindowSize.js` - Hook para obter dimensões da janela
- `useIntersectionObserver.js` - Hook para detectar elementos na viewport
